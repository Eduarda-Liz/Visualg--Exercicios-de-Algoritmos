Algoritmo "ConteDeUmADez"

Var
      Contador, tabuada, resp : inteiro
Inicio
      Contador <- 1
      tabuada <- 7
      Repita
      resp <- Contador * 7
      Escreval (Contador, " x ", tabuada, " = ", resp )
      Contador <- Contador + 1
      Ate (Contador >10)


Fimalgoritmo